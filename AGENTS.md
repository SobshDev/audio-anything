# Repository instructions

Before finishing any code change, you must run `./scripts/check.sh` from the
repository root and resolve every failure. Do not report the work as complete
unless the script passes.
